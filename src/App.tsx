import OpeningScene from "./scenes/OpeningScene";

// Phase 1: perfecting only the opening scene before building the rest of the world.
// See src/scenes/WorldStage.tsx and friends for the (currently unused) earlier
// SVG-based village prototype, kept around for reference.
function App() {
  return <OpeningScene />;
}

export default App;
